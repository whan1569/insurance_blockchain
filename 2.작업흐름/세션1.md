
# ✅ 세션 1: 블록체인 기반 인프라 구축 – 작업 단위

### 🎯 마일스톤 1-1: P2P 네트워크 구현

* [ ] 노드 구조 설계 (ID, IP, 포트 정보 포함)
* [ ] P2P 연결 수립 로직 구현 (`libp2p` 또는 TCP 소켓 사용)
* [ ] 메시지 송수신 처리 (ping, 트랜잭션, 블록 등)
* [ ] 연결된 피어 유지 및 재접속 관리
* [ ] 기본 트랜잭션 전파 기능 구현

---

### 🎯 마일스톤 1-2: 노드 역할 및 구조 설계

* [ ] 노드 역할(Role) 정의 (참여, 검증, 관리)
* [ ] 노드 config 파일에 역할 반영
* [ ] 역할에 따른 처리 분기 (블록 생성 가능 여부 등)
* [ ] 관리 노드 → 인증된 검증 노드 등록 로직

---

### 🎯 마일스톤 1-3: 블록 구조 및 트랜잭션 포맷 구현

* [ ] 트랜잭션 구조 정의 (보낸 사람, 데이터, 서명 등)
* [ ] 블록 구조 정의 (index, timestamp, prev\_hash 등)
* [ ] 트랜잭션 → 블록에 포함 → 체인에 연결 로직
* [ ] 블록 및 트랜잭션 유효성 검증 로직

---

### 🎯 마일스톤 1-4: PoA 합의 알고리즘 구현

* [ ] 인증 노드 목록 저장 방식 설계 (local 또는 중앙 관리)
* [ ] 검증 노드만 블록 생성 허용하는 로직 구현
* [ ] 블록 제안 → 피어들에 전파 → 검증 및 승인
* [ ] 이중 생성 방지 및 충돌 처리 로직

---

### 🎯 마일스톤 1-5: API 서버 구축

* [ ] Flask/FastAPI 서버 구축
* [ ] `/tx`, `/block`, `/chain` 등 기본 REST API 구성
* [ ] P2P 네트워크와 API 간 인터페이스 구성
* [ ] API 호출로 트랜잭션 생성 및 블록 조회 가능 확인

---

### 🎯 마일스톤 1-6: IPFS 연동 테스트

* [ ] IPFS 노드 설정 또는 외부 노드 연동
* [ ] 파일 업로드 및 해시값 반환 확인
* [ ] 해시값 → 블록의 트랜잭션 내 포함 로직
* [ ] IPFS에서 데이터 복원 → 무결성 검증
